{% extends "::base.html.twig" %}
{% block title %}{{ parent() }} - Accueil{% endblock %}
{% block body %}  

  <div class="row">
    <div class="col-lg-12">
      <div class="bs-component">
        <div class="panel panel-default">
          <div class="panel-heading">
            <div class="row">
              <div class="col-md-6 text-left"><h5>Ajoutés récemment</h5></div>
              <div class="col-md-6 text-right">
                <a href="{{ path('jc_retroxboxone_all') }}">Tous les jeux</a> 
              </div>
            </div>
          </div>
          <div class="panel-body panel-height">
            <!--
            <div class="row">
              {% for listLastExitGame in listLastExitGames %}
                <div class="col-sm-6 col-md-2">
                  <div class="thumbnail">
                    <img src="{{ vich_uploader_asset(listLastExitGame, 'imageFile') }}" alt="{{ listLastExitGame.jeuvideo.nom }}" />
                    <div class="caption">
                      <h6>{{ listLastExitGame.jeuvideo.nom }}</h6>
                      <button type="button" class="btn btn-xs btn-primary">80</button>
                      <p></p>
                      <p>
                        <a href="{{ path('jc_retroxboxone_detail', { 'id' : listLastExitGame.id }) }}" class="btn btn-info btn-block" role="button">
                          Détail
                        </a>
                      </p>
                    </div>
                  </div>
                </div>
              {% endfor %}  
            </div>
            <div class="row">
              {% for listLastExitGame in listLastExitGames %}
                <div class="col-sm-6 col-md-2">
                  <p>{{ listLastExitGame.jeuvideo.nom }}</p>
                  <img src="{{ vich_uploader_asset(listLastExitGame, 'imageFile') }}" class="img-responsive" alt="{{ listLastExitGame.jeuvideo.nom }}" /> 
                </div>  
              {% endfor %}  
            </div> 
            <div class="row">
              {% for listLastExitGame in listLastExitGames|slice(0, 6) %}
                <div class="col-lg-2">
                  <img src="{{ vich_uploader_asset(listLastExitGame, 'imageFile') }}" class="img-responsive" alt="{{ listLastExitGame.jeuvideo.nom }}" 
                  />
                  <h6>{{ listLastExitGame.jeuvideo.nom }}</h6>
                  <a href="{{ path('jc_retroxboxone_detail', { 'id' : listLastExitGame.id }) }}">{{ listLastExitGame.jeuvideo.nom }}</a>
                </div>  
              {% endfor %}  
            </div>
            -->
            <div class="row">
              {% for listLastExitGame in listLastExitGames %}
                <div class="col-sm-6 col-md-2">
                  <div class="thumbnail">
                    <img src="{{ vich_uploader_asset(listLastExitGame, 'imageFile') }}" alt="{{ listLastExitGame.jeuvideo.nom }}" />
                    <div class="caption">
                      <a href="{{ path('jc_retroxboxone_detail', { 'id' : listLastExitGame.id }) }}">{{ listLastExitGame.jeuvideo.nom }}</a>
                    </div>
                  </div>
                </div>
              {% endfor %}  
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

{% endblock %}